<UserControl x:Class="GoPlugin.UCOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoPlugin"
             xmlns:ui="clr-namespace:RegExpressLibrary.UI;assembly=RegExpressLibrary"
             mc:Ignorable="d" 
             d:DesignHeight="487.577" d:DesignWidth="705.436" 
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <local:Versions x:Key="versions"/>
        <Style TargetType="ui:CheckboxWithNote">
            <EventSetter Event="Changed" Handler="CheckBox_Changed"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="0 2 4 2"/>
        </Style>
        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBlock" x:Key="note">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{x:Static SystemColors.ControlTextBrush}"/>
            <Setter Property="Opacity" Value="0.70"/>
            <Setter Property="Margin" Value="4 0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Top">

            <Grid Margin="0 0 0 4">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid.LayoutTransform>
                    <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                </Grid.LayoutTransform>

                <Grid.Resources>
                    <Style TargetType="Label">
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Padding" Value="0 1 4 1"/>
                    </Style>
                    <Style TargetType="ComboBox">
                        <Setter Property="HorizontalAlignment" Value="Left" />
                        <Setter Property="VerticalAlignment" Value="Center" />
                        <Setter Property="Padding" Value="4 2"/>
                        <Setter Property="Margin" Value="1"/>
                    </Style>
                </Grid.Resources>

                <Label Grid.Column="0" Grid.Row="0" Content="Package:" Target="{Binding ElementName=cbxPackage}"/>
                <ComboBox x:Name="cbxPackage" Grid.Column="1" Grid.Row="0" MinWidth="77" SelectedValuePath="Tag" SelectedValue="{Binding Package, FallbackValue='regexp'}" SelectionChanged="cbxPackage_SelectionChanged">
                    <ui:ComboBoxItemWithVersion Tag ="regexp"  Text="regexp"  Version="{Binding Regexp, Source={StaticResource versions}}" IsSelected="True"/>
                    <ui:ComboBoxItemWithVersion Tag ="regexp2" Text="regexp2" Version="{Binding Regexp2, Source={StaticResource versions}}"/>
                    <ui:ComboBoxItemWithVersion Tag ="rexa"    Text="rexa"    Version="{Binding Rexa, Source={StaticResource versions}}"/>
                    <ui:ComboBoxItemWithVersion Tag ="coregex" Text="coregex" Version="{Binding Coregex, Source={StaticResource versions}}"/>
                </ComboBox>

            </Grid>

            <!-- regexp2 -->
            <StackPanel x:Name="pnlRegexp2Flags" Orientation="Vertical">
                <ui:CheckboxWithNote Prop="IgnoreCase" Note=""/>
                <ui:CheckboxWithNote Prop="Multiline" Note=""/>
                <ui:CheckboxWithNote Prop="ExplicitCapture" Note=""/>
                <ui:CheckboxWithNote Prop="Singleline" Note=""/>
                <ui:CheckboxWithNote Prop="IgnorePatternWhitespace" Note=""/>
                <ui:CheckboxWithNote Prop="RightToLeft" Note=""/>
                <ui:CheckboxWithNote Prop="ECMAScript" Note=""/>
                <ui:CheckboxWithNote Prop="RE2" Note=""/>
                <ui:CheckboxWithNote Prop="Unicode" Note=""/>
            </StackPanel>

            <!-- rexa -->
            <StackPanel x:Name="pnlRexaFlags" Orientation="Vertical">
                <ui:CheckboxWithNote Prop="IgnoreCase" Text="FlagCaseInsensitive" Note=""/>
                <ui:CheckboxWithNote Prop="Multiline" Text="FlagMultiline" Note=""/>
                <ui:CheckboxWithNote Prop="Singleline" Text="FlagDotAll" Note=""/>
                <ui:CheckboxWithNote Prop="Ungreedy" Text="FlagUngreedy" Note=""/>
                <ui:CheckboxWithNote Prop="Unicode" Text="FlagUnicode" Note=""/>
            </StackPanel>

            <!-- coregex -->
            <StackPanel x:Name="pnlCoregex" Orientation="Vertical">
                <ui:CheckboxWithNote Prop="EnableDFA" Note="enables the Lazy DFA engine; when false, only NFA (PikeVM) is used" />
                <ui:CheckboxWithNote Prop="EnablePrefilter" Note="enables literal-based prefiltering" />
                <ui:CheckboxWithNote Prop="EnableASCIIOptimization" Note="enables ASCII runtime detection; choose between UTF-8 NFA and ASCII NFA"/>

                <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0 4 0 4" >
                    <Grid.LayoutTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Grid.LayoutTransform>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto" MinWidth="55"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Column="0" Grid.Row="0" Target="{Binding ElementName=MaxDFAStates}" Content="MaxDFAStates:"/>
                    <TextBox x:Name="MaxDFAStates" Grid.Column="1" Grid.Row="0" Text="{Binding MaxDFAStates, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="0" Text="– maximum number of DFA states to cache; larger values use more memory but reduce NFA fallback frequency." Style="{StaticResource note}" />

                    <Label Grid.Column="0" Grid.Row="1" Target="{Binding ElementName=DeterminizationLimit}" Content="DeterminizationLimit:"/>
                    <TextBox x:Name="DeterminizationLimit" Grid.Column="1" Grid.Row="1" Text="{Binding DeterminizationLimit, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="1" Text="– the number of NFA states per DFA state; prevents exponential blowup in patterns like (a*)*b" Style="{StaticResource note}" />

                    <Label Grid.Column="0" Grid.Row="2" Target="{Binding ElementName=MinLiteralLen}" Content="MinLiteralLen:"/>
                    <TextBox x:Name="MinLiteralLen" Grid.Column="1" Grid.Row="2" Text="{Binding MinLiteralLen, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="2" Text="– minimum length for prefilter literals; shorter literals may have too many false positives." Style="{StaticResource note}" />

                    <Label Grid.Column="0" Grid.Row="3" Target="{Binding ElementName=MaxLiterals}" Content="MaxLiterals:"/>
                    <TextBox x:Name="MaxLiterals" Grid.Column="1" Grid.Row="3" Text="{Binding MaxLiterals, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="3" Text="– the number of literals to extract for prefiltering" Style="{StaticResource note}" />

                    <Label Grid.Column="0" Grid.Row="4" Target="{Binding ElementName=MaxRecursionDepth}" Content="MaxRecursionDepth:"/>
                    <TextBox x:Name="MaxRecursionDepth" Grid.Column="1" Grid.Row="4" Text="{Binding MaxRecursionDepth, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="4" Text="– limits recursion during NFA compilation" Style="{StaticResource note}" />
                </Grid>

            </StackPanel>

            <ui:CheckboxWithNote x:Name="cbxPosix" Prop="posix_syntax" Text="posix" Note="restrict pattern to POSIX egrep syntax"/>
            <ui:CheckboxWithNote x:Name="cbxLongest" Prop="longest_match" Text="longest" Note="prefer the leftmost-longest match"/>
            <ui:CheckboxWithNote x:Name="cbxLiteral" Prop="literal" Note="interpret pattern as literal, not regexp"/>

        </StackPanel>
    </Grid>
</UserControl>
