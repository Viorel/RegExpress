/*!
 * \file regex.hpp
 * \brief Alias of \c real.hpp — the name a newcomer types first.
 *
 * The public header is `<real/real.hpp>`. This file exists so
 * `#include <real/regex.hpp>` works. `regex_set` and `dfa` stay opt-in
 * includes; they are not pulled in here.
 */
#ifndef REAL_REGEX_HPP
#define REAL_REGEX_HPP

#include "real.hpp"

#endif // REAL_REGEX_HPP
