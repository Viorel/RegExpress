<UserControl x:Class="RealPlugin.UCOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RealPlugin"
             xmlns:ui="clr-namespace:RegExpressLibrary.UI;assembly=RegExpressLibrary"
             mc:Ignorable="d" 
             d:DesignHeight="116.376" d:DesignWidth="767.909" 
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style TargetType="ui:CheckboxWithNote">
            <EventSetter Event="Changed" Handler="CheckBox_Changed"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel Orientation="Vertical" VerticalAlignment="Top">
            <ui:CheckboxWithNote Prop="icase" Note="case-insensitive"/>
            <ui:CheckboxWithNote Prop="multiline" Note="^ and $ also match at line boundaries"/>
            <ui:CheckboxWithNote Prop="dotall" Note=". also matches \n"/>
            <!-- not supported here -->
            <!--<ui:CheckboxWithNote Prop="bytes" Note=". and [^…] match raw bytes, not codepoints"/>-->
            <ui:CheckboxWithNote Prop="verbose" Note="ignore unescaped whitespace and # comments outside classes"/>
            <ui:CheckboxWithNote Prop="ecma" Note="$ (no multiline) matches only at the very end (not before a final \n, the Python default), and . (no dotall) also excludes \r"/>
            <ui:CheckboxWithNote Prop="ascii" Note="\d \w \s \b stay ASCII and icase folds ASCII only, even in text mode. ., explicit classes and UTF-8 literals stay code-point-aware"/>
        </StackPanel>
    </Grid>
</UserControl>
