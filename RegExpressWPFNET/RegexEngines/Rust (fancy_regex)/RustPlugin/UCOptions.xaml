<UserControl x:Class="RustPlugin.UCOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RustPlugin"
             xmlns:ui="clr-namespace:RegExpressLibrary.UI;assembly=RegExpressLibrary"
             mc:Ignorable="d" 
             d:DesignHeight="228.8" d:DesignWidth="470.4" 
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <Style TargetType="ui:CheckboxWithNote">
            <EventSetter Event="Changed" Handler="CheckBox_Changed"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0 0 0 4">
                <StackPanel.LayoutTransform>
                    <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                </StackPanel.LayoutTransform>
                <Label Content="Struct:" Target="{Binding ElementName=cbxStruct}" Padding="0 2 4 2"/>
                <ComboBox x:Name="cbxStruct" SelectedValuePath="Tag" SelectedValue="{Binding struct, FallbackValue='Regex'}" VerticalAlignment="Center" SelectionChanged="cbxStruct_SelectionChanged" MinWidth="170">
                    <ComboBoxItem Tag ="Regex" Content="fancy_regex::Regex" IsSelected="True" />
                    <ComboBoxItem Tag ="RegexBuilder" Content="fancy_regex::RegexBuilder" />
                </ComboBox>
            </StackPanel>

            <StackPanel x:Name="pnlRegexBuilderOptions">

                <ui:CheckboxWithNote Prop="case_insensitive" Note="(i) match both upper case and lower case variants"/>

                <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0 6 0 0">
                    <Grid.LayoutTransform>
                        <TransformGroup>
                            <ScaleTransform ScaleX="0.9" ScaleY="0.9"/>
                            <SkewTransform/>
                            <RotateTransform/>
                            <TranslateTransform/>
                        </TransformGroup>
                    </Grid.LayoutTransform>
                    <Grid.Resources>
                        <Style TargetType="Label">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="0 2 4 2"/>
                        </Style>
                        <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Foreground" Value="{x:Static SystemColors.ControlTextBrush}"/>
                            <Setter Property="Opacity" Value="0.70"/>
                            <Setter Property="Margin" Value="4 0"/>
                            <Setter Property="TextWrapping" Value="Wrap"/>
                        </Style>
                    </Grid.Resources>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="44"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Column="0" Grid.Row="0" Target="{Binding ElementName=bl}">backtrack__limit:</Label>
                    <TextBox x:Name="bl" Grid.Column="1" Grid.Row="0" Text="{Binding backtrack_limit, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="0" Text="– limit for how many times backtracking should be attempted" />

                    <Label Grid.Column="0" Grid.Row="1" Target="{Binding ElementName=dsl}">delegate__size__limit:</Label>
                    <TextBox x:Name="dsl" Grid.Column="1" Grid.Row="1" Text="{Binding delegate_size_limit, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="1" Text="– approximate size limit of compiled regular expression" />

                    <Label Grid.Column="0" Grid.Row="2" Target="{Binding ElementName=ddsl}">delegate__dfa__size__limit:</Label>
                    <TextBox x:Name="ddsl" Grid.Column="1" Grid.Row="2" Text="{Binding delegate_dfa_size_limit, UpdateSourceTrigger=PropertyChanged}" TextChanged="tb_TextChanged"/>
                    <TextBlock Grid.Column="2" Grid.Row="2" Text="– approximate size of cache used by DFA" />

                </Grid>
            </StackPanel>
        </StackPanel>

    </Grid>
</UserControl>
